\begin{table}[!t] 
    \centering
    \caption{\textbf{Shape} scores (higher is better). Higher scores reflect superior performance. When the value is not reported, it means that the method was not able to generate valid samples on that dataset, usually because of out-of-memory errors.
    } 
    \label{tbl:shape}
    %\small
    \begin{threeparttable}
    {
    % \scalebox{0.92}
    \resizebox{\columnwidth}{!}
    {

    \input{tables/repo_tables/shape}
              }
              }
    \vspace{-5pt}
    \end{threeparttable}

\end{table}